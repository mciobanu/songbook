export type SongRenderConfig = {
    fontSize: number,
    showChords: boolean,
    inlineChords: boolean,
    minNote: string,
    maxNote: string,
    maxSuggestions: number,
    maxCapo: number,
};