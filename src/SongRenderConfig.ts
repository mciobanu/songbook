export type SongRenderConfig = {
    fontSize: number,
    showChords: boolean,
    inlineChords: boolean,
    useSuggestions: boolean,
    minNote: string,
    maxNote: string,
    noteRange: number,
    maxSuggestions: number,
    maxCapo: number,
};
